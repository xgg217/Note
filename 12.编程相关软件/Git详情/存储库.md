# 存储库

## 保存到存储库

+ 暂存区有东西，或者工作区有更改
+ 将目前的状态保存到存储库

  ```bash
  git stash push -u -a
  ```

## 恢复工作区

+ 列出所有存储

  ```bash
  git stash list
  ```

+ 恢复到上一次的存储，同时删除存储

  ```bash
  git stash pop
  ```

+ 恢复到指定存储，同时删除存储

  ```bash
  git stash pop <stash-id>
  ```
