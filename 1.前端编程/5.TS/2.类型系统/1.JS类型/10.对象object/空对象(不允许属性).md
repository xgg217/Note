# 空对象(不允许属性)

## 错误写法

+ 错误写法

  ```ts
  // 实际上表示：任何非 null 或 undefined 的值
  const obj: {} = {};

  // 导致以下使用都是合法的 😳
  const obj: {} = "hello";
  const obj2: {} = 42;
  ```

## 正确的写法

+ 正确的写法

  ```ts
  const obj: Record<string, never> = {};
  ```

+ 现在它真正表示：

  + 不允许任何属性

  + 严格的空对象


