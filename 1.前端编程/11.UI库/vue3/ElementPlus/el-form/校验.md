# 校验

## 方式1 Promise

+ Promise

  ```js
  const formEl = ruleFormRef.value;
  if (!formEl) return;

  formEl
    .validate()
    .then(res => {
      console.log(res);
    })
    .catch(err => {
      // 校验不通过在此触发报错
      console.error(err);
    });
  ```
