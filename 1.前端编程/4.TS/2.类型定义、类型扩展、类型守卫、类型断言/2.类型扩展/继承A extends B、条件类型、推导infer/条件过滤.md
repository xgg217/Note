# 条件过滤

## never

+ never

  ```js
  type Filter<T, Match> = T extends Match ? T : never;

  Filter<string | number>, number> // number
  Filter<"cat" | 42>, string> // "cat"
  Filter<"cat" | 42>, number> // 42
  Filter<"cat" | "dog" | 42>, number> // "cat" | "dog"
  ```
