# 过滤空值

## Object.entries + fromEntries：对象数组互转，过滤空值

+ 先拆键值对数组，过滤后再拼回对象，比 reduce 拼接更直观，URL 参数清洗场景一步完成

  ```js
  const clean = Object.fromEntries(
    Object.entries({ a: 1, b: '', c: 3 }).filter(([, v]) => v)
  );
  // { a: 1, c: 3 }
  ```
