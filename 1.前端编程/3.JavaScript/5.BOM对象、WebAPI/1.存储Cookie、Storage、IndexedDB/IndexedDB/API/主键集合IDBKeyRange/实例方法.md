# 实例方法

## includes

+ 语法 `includes(key)`

+ 参数

  + key 你要检查的密钥范围内的密钥。这可以是任何类型

  ```js
  var keyRangeValue = IDBKeyRange.bound('A', 'K', false, false);

  keyRangeValue.includes('F') // true
  keyRangeValue.includes('W') // false
  ```
