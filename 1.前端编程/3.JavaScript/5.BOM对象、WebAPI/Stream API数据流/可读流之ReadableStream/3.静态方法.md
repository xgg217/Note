# 静态方法

## ReadableStream：from()

+ ReadableStream.from() 静态方法从提供的可迭代或异步可迭代对象返回一个 ReadableStream。

  + 该方法可以用于将可迭代或异步可迭代对象包装为可读流，包括数组、集合、promise 数组、异步生成器、ReadableStream、Node.js 可读流，等等

+ 语法 `ReadableStream.from(anyIterable)`

+ 参数

  + anyIterable 一个可迭代或异步可迭代对象

+ 返回值

  + 一个 ReadableStream
