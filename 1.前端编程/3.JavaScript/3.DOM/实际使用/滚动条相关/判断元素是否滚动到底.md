# 判断元素是否滚动到底

## code

+ scrollTop 是一个非整数，而 scrollHeight 和 clientHeight 是四舍五入的，因此确定滚动区域是否滚动到底的唯一方法是查看滚动量是否足够接近某个阈值

  ```js
  Math.abs(element.scrollHeight - element.clientHeight - element.scrollTop) < 1;
  ```
