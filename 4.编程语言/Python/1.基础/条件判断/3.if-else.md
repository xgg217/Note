# if-else

## 语法

+ 语法

  ```py
  if 条件表达式:
      语句/语句块1
  else:
      语句/语句块2

  # or
  if(条件表达式):
    语句/语句块1
  else:
    语句/语句块2
  ```

  ```py
  num = 6

  if(num > 5):
      print("大于5:" + str(num))
  else:
      print("小于5:" + str(num))
  ```
