# 项目规则

## 项目规则设置

+ 顾名思义就是每个项目一份，项目之间不会共享

+ 在项目根目录的 `.cursor/rules` 文件夹中创建 `.mdc` 文件
+ mdc 是 Cursor 的 Rules 文件的专有格式
+ 它基于 Markdown 格式
+ 这意味着你直接使用最熟悉的 Markdown 语法来写就完事儿了

## 支持规则类型

+ 方式1 Always：自动附加到所有聊天和命令请求（command + k），适用于全局规则
+ 方式2 Auto Attached：基于文件模式匹配自动触发，例如: .tsx, .json, Test.cpp
+ 方式3 Agent Requested：针对特定任务场景，需要提供任务描述。帮助 AI Agent 更好地理解和执行特定任务
+ 方式4 Manual：需要手动提及才会被包含，适用于特殊场景的临时规则

  ![alt text](images/项目规则.png)

+ 支持的功能：

  + 规则内容：**用 Markdown 格式**写规则
  + 规则引用：用 @file 引用其他规则文件

  ![alt text](images/项目规则支持的功能.png)
